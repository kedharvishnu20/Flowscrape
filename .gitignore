bin/
node_modules/

# Playwright's browser download; this repo uses the pre-installed one
test-results/

# Scratch git worktrees the agent harness creates
.claude/worktrees/

# Scratch scripts the MCP runner writes and removes.
.fs-mcp-runs/
